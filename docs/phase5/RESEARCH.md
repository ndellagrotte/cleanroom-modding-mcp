# Phase 5 relevant research (missing)

Agent must initiate recon session that reads the existing code and emits findings with file:line references and open questions — no code written. Fan this out to subagents so the exploration cost stays in their context, not yours
